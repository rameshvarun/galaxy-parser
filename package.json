{
  "name": "galaxy-parser",
  "devDependencies": {
    "chai": "^3.4.1",
    "eslint": "^1.9.0",
    "fs-walk": "0.0.1",
    "jison": "^0.4.15",
    "mocha": "^2.3.3"
  },
  "dependencies": {},
  "scripts": {
    "build" : "jison lib/Galaxy.jison -o lib/index.js -m commonjs -p lr",
    "test": "mocha test/",
    "prepublish": "npm run build",
    "pretest": "npm run build"
  }
}
